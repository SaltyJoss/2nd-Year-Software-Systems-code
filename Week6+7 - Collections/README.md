# Week 6/7 - Collections

## Lab 6:
  ### Explanation:
    - NameManager class to deal with storing the names in an Array List (visiting for first time in module)
    - addName(String s) method that adds names to the list
    - printNames() method that iterates (think Big O for optimising algorithm) over the names and prints to console
    - removeLongNames() that iterates over the list and removes and names longer than set paramter (len)
    - Defining in README.md as each method deals with a different part of the code, whilst small practice is good
    - Very useful for more critical thinking, especially when relating to older project, the ability to ustilise a more 'effective' array-type
    - In Driver class, we create instances of the names, by creating one object of the class and re-calling the addName(String s) method to add the names
    - Driver shows, again, useful nature of the code
  ### Breakdown:
  

## Lab 7:
  ### Explanation:
    - NumberSorter class demostrates two stack-types collections can be used for a simple sorting algorithm
  ### Breakdown:
